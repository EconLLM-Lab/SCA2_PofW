# P1 criterion-recovery network — GPS posrecip as the criterion (starter template).
# AUGUSTO OWNS: theta, delta, restrictions. Bridge items; low correlations expected.
name: p1_criterion_posrecip_template
delta: 0.0
restrictions:
  - id: r_corr_min_gps_posrecip
    type: corr_min
    theta: 0.30
    params:
      variable: gps_posrecip
