# P1 criterion-recovery network — GPS risktaking as the criterion (starter template).
# AUGUSTO OWNS: theta, delta, restrictions. The mapped WVS items are economic
# values, NOT lottery/staircase risk; near-zero correlations expected. This run
# tests whether they are admissible GPS-risk operationalizations at ANY threshold.
name: p1_criterion_risktaking_template
delta: 0.0
restrictions:
  - id: r_corr_min_gps_risktaking
    type: corr_min
    theta: 0.30
    params:
      variable: gps_risktaking
