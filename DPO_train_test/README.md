The files need to be run in the following order:

1. Preprocessing
2. Training
3. Evaluation

As input, the first notebook takes the D_syn files from synthetic generation, plus a file containing extended answer options.
Before running, modify the file paths.
All notebooks except for preprocessing_without_gpu require GPU.
