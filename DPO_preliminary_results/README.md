This is a folder with notebooks for training the DPO algorithm, evaluating it and storing the results.

The training is presented in DPO_train_eval.ipynb, the resulting .csv files are also attached. In order to run the file, modify the paths as needed.

The comments on the results are attached in Results_EDA.ipynb
